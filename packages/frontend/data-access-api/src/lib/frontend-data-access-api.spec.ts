import { frontendDataAccessApi } from './frontend-data-access-api';

describe('frontendDataAccessApi', () => {
  it('should work', () => {
    expect(frontendDataAccessApi()).toEqual('frontend-data-access-api');
  });
});
